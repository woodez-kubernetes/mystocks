replicaCount: 1
image:
  repository: kwood475/mystocks
  tag: release-dev-46e8607
  pullPolicy: Always
namespace: woodez-database
ingress:
  enabled: true
  className: nginx
  host: mystocks.apexkube.xyz
database:
  host: postgres-svc
  port: "5432"
  name: mystocks
  user: mystocks
  password: mystocks2026
smtp:
  # Pre-created K8s Secret name containing EMAIL_HOST_USER, EMAIL_HOST_PASSWORD, REPORT_RECIPIENT_EMAIL
  existingSecret: mystocks-smtp-secret
django:
  secretKey: "change-me-in-production"
  debug: "False"
  allowedHosts: "mystocks.apexkube.xyz,localhost"
ollama:
  host: "http://ollama-svc:11434"
scheduler:
  interval: "60"
